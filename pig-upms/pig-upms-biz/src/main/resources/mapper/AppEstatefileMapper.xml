<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pig4cloud.pig.admin.mapper.AppEstatefileMapper">

  <resultMap id="appEstatefileMap" type="com.pig4cloud.pig.admin.api.entity.AppEstatefileEntity">
        <id property="estatefileId" column="estatefile_id"/>
        <result property="estatefileName" column="estatefile_name"/>
        <result property="bucketName" column="bucket_name"/>
        <result property="original" column="original"/>
        <result property="type" column="type"/>
        <result property="fileSize" column="file_size"/>
        <result property="estateId" column="estate_id"/>
        <result property="suiteId" column="suite_id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
  </resultMap>


	<!-- 通过建筑ID，查询图片信息-->
	<select id="listImageByEstateId" resultMap="appEstatefileMap">
		SELECT *
		FROM app_estatefile
		WHERE app_estatefile.estate_id = #{estateId}
			AND (app_estatefile.type = 'png' or app_estatefile.type = 'jpg' or app_estatefile.type = 'jpeg')
	</select>

	<!-- 通过建筑ID，查询视频信息-->
	<select id="listVideoByEstateId" resultMap="appEstatefileMap">
		SELECT *
		FROM app_estatefile
		WHERE app_estatefile.estate_id = #{estateId}
			AND (
			    	app_estatefile.type = 'mp4'
			        or app_estatefile.type = 'avi'
			        or app_estatefile.type = 'wmv'
			        or app_estatefile.type = 'flv'
			        or app_estatefile.type = 'rmvb'
			        or app_estatefile.type = '3gp'
			    )
	</select>

	<!-- 通过房屋ID，查询图片信息-->
	<select id="listImageBySuiteId" resultMap="appEstatefileMap">
		SELECT *
		FROM app_estatefile
		WHERE app_estatefile.suite_id = #{SuiteId}
			AND (app_estatefile.type = 'png' or app_estatefile.type = 'jpg' or app_estatefile.type = 'jpeg')
	</select>

	<!-- 通过房屋ID，查询视频信息-->
	<select id="listVideoBySuiteId" resultMap="appEstatefileMap">
		SELECT *
		FROM app_estatefile
		WHERE app_estatefile.suite_id = #{SuiteId}
		  AND (
				app_estatefile.type = 'mp4'
				or app_estatefile.type = 'avi'
				or app_estatefile.type = 'wmv'
				or app_estatefile.type = 'flv'
				or app_estatefile.type = 'rmvb'
				or app_estatefile.type = '3gp'
				)
	</select>
</mapper>